import { notFound } from "next/navigation";
import { ClipboardList } from "lucide-react";
import RestaurantOrderActions from "@/components/admin/RestaurantOrderActions";
import ServiceCallsBoard from "@/components/service/ServiceCallsBoard";
import { isSuperAdmin } from "@/lib/auth/isSuperAdmin";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { getRestaurant } from "@/lib/db/restaurants/getRestaurant";
import { getRestaurantOrders } from "@/lib/db/restaurants/orders/getRestaurantOrders";
import type { Order, OrderStatus } from "@/types/orders";

export const dynamic = "force-dynamic";
type Props = { params: Promise<{ slug: string }> };
const statusLabels: Record<OrderStatus, string> = { pending: "Recibido", preparing: "Preparando", ready: "Listo", served: "Servido", bill: "Cuenta", completed: "Finalizado", cancelled: "Cancelado" };
const statusClasses: Record<OrderStatus, string> = { pending: "border-yellow-500/30 bg-yellow-500/15 text-yellow-300", preparing: "border-blue-500/30 bg-blue-500/15 text-blue-300", ready: "border-emerald-500/30 bg-emerald-500/15 text-emerald-300", served: "border-emerald-500/30 bg-emerald-500/15 text-emerald-300", bill: "border-amber-500/30 bg-amber-500/15 text-amber-300", completed: "border-zinc-600 bg-zinc-700/60 text-zinc-200", cancelled: "border-red-500/30 bg-red-500/15 text-red-300" };
type DisplayOrder = Order & { sourceOrderIds?: string[] };
type OrderSection = { key: string; title: string; description: string; orders: DisplayOrder[]; accent: string; dot: string };
function sortByCreatedAt(orders: DisplayOrder[]) { return [...orders].sort((a, b) => new Date(a.created_at).getTime() - new Date(b.created_at).getTime()); }
function groupServedByTable(orders: Order[]): DisplayOrder[] { const grouped = new Map<string, DisplayOrder>(); for (const order of sortByCreatedAt(orders)) { const existing = grouped.get(order.table_number); if (!existing) { grouped.set(order.table_number, { ...order, sourceOrderIds: [order.id] }); continue; } existing.sourceOrderIds = [...(existing.sourceOrderIds ?? [existing.id]), order.id]; existing.order_items = [...existing.order_items, ...order.order_items]; existing.total += Number(order.total); existing.notes = [existing.notes, order.notes].filter(Boolean).join("\n"); existing.created_at = order.created_at; existing.status = order.status; } return sortByCreatedAt(Array.from(grouped.values())); }

export default async function OrdersPage({ params }: Props) {
  const { slug } = await params; const supabase = await createSupabaseServerClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) notFound(); const restaurant = await getRestaurant(slug); if (!restaurant) notFound(); const superAdmin = await isSuperAdmin(user.id);
  if (!superAdmin) { const { data: membership, error } = await supabase.from("restaurant_users").select("restaurant_id, role").eq("user_id", user.id).eq("restaurant_id", restaurant.id).in("role", ["owner", "staff"]).maybeSingle(); if (error) throw error; if (!membership) notFound(); }
  const orders = await getRestaurantOrders(restaurant.id); const received = orders.filter((o) => o.status === "pending"); const preparing = orders.filter((o) => o.status === "preparing" || o.status === "ready"); const served = groupServedByTable(orders.filter((o) => o.status === "served" || o.status === "bill")); const historical = orders.filter((o) => o.status === "completed" || o.status === "cancelled");
  const activeForPayment = orders.filter((o) => ["pending", "preparing", "ready", "served", "bill"].includes(o.status));
  const tableGroups = new Map<string, { ids: string[]; total: number }>();
  for (const order of activeForPayment) { const g = tableGroups.get(order.table_number) ?? { ids: [], total: 0 }; g.ids.push(order.id); g.total += Number(order.total ?? 0); tableGroups.set(order.table_number, g); }
  const sections: OrderSection[] = [
    { key: "received", title: "Recibido", description: "Pedidos nuevos", orders: sortByCreatedAt(received), accent: "border-yellow-500/30 bg-yellow-500/[0.055]", dot: "bg-yellow-400" },
    { key: "preparing", title: "Preparando", description: "En cocina o listos", orders: sortByCreatedAt(preparing), accent: "border-blue-500/30 bg-blue-500/[0.055]", dot: "bg-blue-400" },
    { key: "served", title: "Servido", description: "Agrupado por mesa", orders: served, accent: "border-emerald-500/30 bg-emerald-500/[0.055]", dot: "bg-emerald-400" },
  ];
  const activeCount = received.length + preparing.length + served.length;
  return <main className="min-h-screen bg-[#11100f] text-white"><div className="mx-auto max-w-[1800px] px-3 py-4 sm:px-5 sm:py-6 lg:px-7"><header className="mb-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl border border-amber-500/25 bg-amber-500/10 text-amber-400"><ClipboardList size={20} strokeWidth={1.8} /></div><span className="text-[10px] font-semibold uppercase tracking-[0.28em] text-amber-500">Cocina · Operativa</span></div><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Pedidos</h1><p className="mt-1 text-sm text-zinc-400">{restaurant.name} · panel de pedidos en tiempo real</p></div><div className="flex gap-2"><div className="rounded-xl border border-zinc-800 bg-[#181716] px-4 py-2.5"><p className="text-[9px] uppercase tracking-[0.18em] text-zinc-500">Activos</p><p className="mt-0.5 text-xl font-bold text-white">{activeCount}</p></div><div className="rounded-xl border border-zinc-800 bg-[#181716] px-4 py-2.5"><p className="text-[9px] uppercase tracking-[0.18em] text-zinc-500">Histórico</p><p className="mt-0.5 text-xl font-bold text-white">{historical.length}</p></div></div></header><ServiceCallsBoard restaurantId={restaurant.id} /><div className="mb-5 h-px bg-gradient-to-r from-amber-500/50 via-zinc-700 to-transparent" />{orders.length === 0 ? <div className="rounded-2xl border border-zinc-800 bg-[#181716] px-6 py-20 text-center"><ClipboardList className="mx-auto h-12 w-12 text-zinc-700" /><h2 className="mt-5 text-xl font-semibold text-white">No hay pedidos todavía</h2><p className="mt-2 text-sm text-zinc-500">Cuando un cliente haga un pedido aparecerá aquí.</p></div> : <><div className="grid gap-4 xl:grid-cols-3 xl:items-start">{sections.map((section) => <section key={section.key} className={`min-w-0 rounded-2xl border p-3 sm:p-4 ${section.accent}`}><div className="sticky top-2 z-10 mb-4 rounded-xl border border-zinc-700/80 bg-[#1a1917]/95 px-4 py-3.5 shadow-lg backdrop-blur"><div className="flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><span className={`h-3 w-3 shrink-0 rounded-full ${section.dot}`} /><div className="min-w-0"><h2 className="text-lg font-bold text-white">{section.title}</h2><p className="truncate text-[11px] text-zinc-500">{section.description}</p></div></div><span className="flex h-9 min-w-9 shrink-0 items-center justify-center rounded-full border border-zinc-600 bg-[#11100f] px-2 text-sm font-bold text-white">{section.orders.length}</span></div></div>{section.orders.length === 0 ? <div className="flex min-h-[180px] items-center justify-center rounded-xl border border-dashed border-zinc-700/70 bg-[#171614]/80 px-5 py-8 text-center text-sm text-zinc-600">No hay pedidos</div> : <div className="space-y-3">{section.orders.map((order, index) => <OrderCard key={order.id} order={order} slug={slug} restaurantName={restaurant.name} alternate={index % 2 === 0} tableGroup={tableGroups.get(order.table_number)} />)}</div>}</section>)}</div>{historical.length > 0 && <section className="mt-6 rounded-2xl border border-zinc-800 bg-[#151413] p-4 sm:p-5"><div className="mb-4 flex items-end justify-between gap-4 border-b border-zinc-800/80 pb-4"><div><h2 className="text-lg font-semibold text-zinc-300">Histórico</h2><p className="mt-1 text-xs text-zinc-600">Pedidos finalizados o cancelados</p></div><span className="rounded-full border border-zinc-800 bg-[#181716] px-3 py-1 text-xs text-zinc-500">{historical.length}</span></div><div className="grid gap-3 lg:grid-cols-2">{sortByCreatedAt(historical).reverse().map((order, index) => <OrderCard key={order.id} order={order} slug={slug} restaurantName={restaurant.name} compact alternate={index % 2 === 0} />)}</div></section>}</>}</div></main>;
}

function OrderCard({ order, slug, restaurantName, compact = false, alternate = false, tableGroup }: { order: DisplayOrder; slug: string; restaurantName: string; compact?: boolean; alternate?: boolean; tableGroup?: { ids: string[]; total: number } }) {
  const itemCount = order.order_items.reduce((sum, item) => sum + item.quantity, 0); const cardTone = alternate ? "border-orange-400/35 bg-orange-500/[0.10]" : "border-zinc-600/80 bg-[#292724]"; const headerTone = alternate ? "border-orange-400/25 bg-orange-500/[0.075]" : "border-zinc-600/80 bg-[#322f2b]"; const itemTone = alternate ? "border-orange-400/20 bg-orange-500/[0.055]" : "border-zinc-600/70 bg-[#37332f]";
  return <article className={`overflow-hidden rounded-2xl border shadow-[0_12px_30px_rgba(0,0,0,0.28)] ring-1 ring-black/20 ${cardTone}`}><div className={`border-b px-4 py-3.5 sm:px-5 ${headerTone}`}><div className="flex items-center justify-between gap-3"><div className="flex min-w-0 items-center gap-3"><div className="flex h-14 min-w-[92px] items-center justify-center gap-1.5 rounded-xl border-2 border-amber-400/50 bg-amber-400/10 px-3 shadow-inner shadow-amber-500/10"><span className="text-[9px] font-bold uppercase tracking-[0.18em] text-amber-400">Mesa</span><span className="text-3xl font-black leading-none tracking-tight text-white">{order.table_number}</span></div><div className="min-w-0"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-zinc-500">{order.sourceOrderIds && order.sourceOrderIds.length > 1 ? `${order.sourceOrderIds.length} pedidos agrupados` : "Pedido"}</p><p className="mt-0.5 truncate text-xs text-zinc-400">{order.sourceOrderIds && order.sourceOrderIds.length > 1 ? "Todo lo servido de esta mesa" : `#${order.id.slice(0, 8)} · ${new Date(order.created_at).toLocaleString("es-ES")}`}</p></div></div><span className={`shrink-0 rounded-full border px-3 py-1.5 text-[11px] font-bold ${statusClasses[order.status]}`}>{statusLabels[order.status]}</span></div></div><div className="p-4 sm:p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-zinc-500">Artículos</p><p className="mt-1 text-base font-semibold text-zinc-100">{itemCount}</p></div><div className="text-left sm:text-right"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-zinc-500">Total</p><p className="mt-0.5 text-3xl font-black tracking-tight text-white">{order.total.toFixed(2)} €</p></div></div>{!compact && <><div className="my-4 h-px bg-zinc-600/70" /><div className="grid gap-2.5 sm:grid-cols-2">{order.order_items.map((item) => <div key={item.id} className={`rounded-lg border px-3.5 py-3 ${itemTone}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-sm font-semibold text-white">{item.quantity} × {item.name}</p>{item.options.length > 0 && <p className="mt-1 text-xs leading-5 text-zinc-400">{item.options.map((option) => option.optionName).join(" · ")}</p>}</div><span className="shrink-0 text-xs font-medium text-zinc-300">{(item.price * item.quantity).toFixed(2)} €</span></div></div>)}</div>{order.notes && <div className="mt-3 rounded-lg border border-amber-500/20 bg-amber-500/10 px-3.5 py-2.5"><p className="text-[10px] font-bold uppercase tracking-wider text-amber-400">Nota</p><p className="mt-1 text-sm text-zinc-200">{order.notes}</p></div>}</>}{!compact && <div className="mt-4 flex flex-col gap-3 border-t border-zinc-600/70 pt-4 sm:flex-row sm:items-center sm:justify-between"><p className="text-[10px] text-zinc-500">Pedido de {restaurantName}</p><RestaurantOrderActions slug={slug} orderId={order.id} orderIds={order.sourceOrderIds ?? []} status={order.status} total={order.total} tableOrderIds={tableGroup?.ids} tableTotal={tableGroup?.total} /></div>}</div></article>;
}
